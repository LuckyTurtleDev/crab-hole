[package]
name = "crab-hole"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
tokio = { version = "1.27.0", features = ["parking_lot", "rt-multi-thread", "macros", "net"] }
trust-dns-server = { version = "0.22.0", features = ["dnssec-ring", "dns-over-rustls", "dns-over-quic"] }
